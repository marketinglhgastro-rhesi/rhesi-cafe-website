---
// TODO: Fotos sind Unsplash-Platzhalter. Vor Launch durch eigene Rhesi-Fotos ersetzen.
const photos = [
  { src: "/images/atmosphaere-1.jpg", alt: "Gemütliche Sitzecke im Rhesi" },
  { src: "/images/atmosphaere-2.jpg", alt: "Gastraum im Rhesi" },
  { src: "/images/atmosphaere-3.jpg", alt: "Bar-Bereich im Rhesi" },
  { src: "/images/atmosphaere-4.jpg", alt: "Gemütlicher Fensterplatz im Rhesi" },
];
---

<section class="bg-cream py-20">
  <div class="mx-auto max-w-6xl px-6">
    <div data-reveal="up" class="text-center">
      <h2 class="font-heading text-3xl font-semibold text-brown">
        Ein Blick ins Rhesi
      </h2>
      <p class="mt-3 text-espresso/70">
        Gemütliche Atmosphäre mitten in Lustenau.
      </p>
    </div>

    <div class="mt-10 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {
        photos.map((photo, index) => (
          <div
            data-reveal="up"
            style={`transition-delay: ${index * 100}ms`}
            class="group h-64 overflow-hidden rounded-2xl shadow-sm ring-1 ring-brown/10"
          >
            <img
              src={photo.src}
              alt={photo.alt}
              loading="lazy"
              class="h-full w-full object-cover transition-transform duration-500 ease-out group-hover:scale-105"
            />
          </div>
        ))
      }
    </div>
  </div>
</section>
